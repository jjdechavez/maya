Maya basic example

Run locally:

1) pnpm install
2) pnpm build
3) pnpm run tamsi dev --config examples/basic/tamsi.config.ts

Fallback:

- node dist/cli.mjs dev --config examples/basic/tamsi.config.ts

Then open http://localhost:5555
